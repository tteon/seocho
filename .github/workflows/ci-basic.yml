# Legacy CI — kept for backward compatibility.
# The canonical CI workflow is ci.yml.
# This file will be removed in a future release.
name: Basic CI (legacy)

on:
  workflow_dispatch:

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use ci.yml instead. This workflow is deprecated."
