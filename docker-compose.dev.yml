services:
  extraction-service:
    volumes:
      - ./extraction:/app
      - ./runtime:/app/runtime:ro
      - ./src/seocho:/app/seocho:ro
      - ./notebooks:/app/notebooks
      - ./demos:/app/demos
