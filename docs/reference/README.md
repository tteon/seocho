# Reference Docs

This directory stores advanced design references that are useful after the
first user path is clear.

These documents are not the primary onboarding path. Promote material back to
the root docs only when it becomes a public user guide, an active operator
contract, or a required contributor entry point.
