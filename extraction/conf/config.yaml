# Non-Hydra reference config for extraction batch runtime.
# Runtime loading is env-first via `config.load_pipeline_runtime_config()`.

model: gpt-4o-mini
mock_data: true
enable_rule_constraints: true

prompts:
  extraction: conf/prompts/default.yaml
  linking: conf/prompts/linking.yaml
