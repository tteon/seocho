---
draws_on:
  - log2026.merge_key_reality.v1
---

# Without a schema, the output depends on which model ran

Three models given one document key the same fact differently, so the graphs
cannot join. What is still missing here is a second run of one model: without
it, between-model disagreement cannot be separated from the variance a single
model has with itself.

_Not written yet. The contracts above are the artifacts this section is allowed to quote; `experiments/check_narrative.py` enforces that and will reject a number from anywhere else._
