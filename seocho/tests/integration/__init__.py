"""SEOCHO integration tests — require live external dependencies.

Tests here are skipped automatically when the relevant service is
unavailable. They are not on the default pytest discovery path; run
explicitly with ``pytest seocho/tests/integration/`` or via marker
(e.g. ``pytest -m integration_gopts``).
"""
